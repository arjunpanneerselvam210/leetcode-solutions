class Solution {
public:
    bool uniqueOccurrences(vector<int>& arr) {
        unordered_map<int,int>m;
        int n = arr.size();
        for(auto i = 0 ;i < n; i++)
            m[arr[i]]++;
        unordered_set<int>s;
        for(auto i : m)
            s.insert(i.second);
        return m.size() == s.size();
    }
};

//
