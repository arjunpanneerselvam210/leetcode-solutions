class Solution {
public:
    bool check(vector<int>& nums) {
       int n = nums.size();
       int cnt = 0;
       for(int i = 0; i < n-1; i++){
        if(nums[i] > nums[i+1]) cnt++;
        if(cnt>1)   return false;
       }
       if(nums[n-1] > nums[0])  cnt++;
       return cnt<2;
    }
};
// to check if the array is sorted and Rotated you have to check the count of arr[i] > arr[i+1]
// if it incremented two times in the loop return false else continue 
// Atlast check another condition after iterating the loop ie the last element is greater than the element in the 0th index
// if it is true increment the cnt
// then return the condition if cnt < 2 (true) otherwise false
eg : [3,4,5,1,2] condition true at i = 2 (cnt = 1) then no other pointers satisfies the condition so true
     [2,1,3,4]   condition true at i = 0 (cnt = 1) then after loop condition(cnt = 2) so false
